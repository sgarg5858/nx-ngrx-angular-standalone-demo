import { TransformAddressPipe } from './transform-address.pipe';

describe('TransformAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
